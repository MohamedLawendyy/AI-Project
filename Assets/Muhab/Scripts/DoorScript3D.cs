using UnityEngine;

public class DoorScript3D : MonoBehaviour
{
    public Transform doorPushTarget; // the position the door should move to when opening
    public float openSpeed = 2f; // how fast the door opens

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }
}
